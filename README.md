# custom-eda-tool-with-fault-injection
Download all the necessity python libraries using pip
The test bench needs to be re written for every different circuits
The Rtl level fault injection is done so the code must use standard forms 
The future improvements will involve universal test bench to yield maximum test coverage 
And the gate level netlist fault injection using Yosys
